<?php
return [
    /*
    |--------------------------------------------------------------------------
    | Redis配置文件
    |--------------------------------------------------------------------------
    */

    /**
     * @see https://packagist.org/packages/predis/predis
     */

    //默认连接标识
    'default' => $_ENV['redis']['default connection'],

    /*
    |--------------------------------------------------------------------------
    | 集群模式
    |--------------------------------------------------------------------------
    */
    'cluster'   =>  [
        'options'   => $_ENV['redis']['cluster']['options'],

        'servers'   => $_ENV['redis']['cluster']['servers'],
    ],

    /*
    |--------------------------------------------------------------------------
    | 单服务器模式
    |--------------------------------------------------------------------------
   */
    'single'     =>  [
        'options'   => $_ENV['redis']['single']['options'],

        'servers'   => $_ENV['redis']['single']['servers'],
    ]
];